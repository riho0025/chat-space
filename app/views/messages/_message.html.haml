.chat-main__messages
    .chat-main__messages__info
    %p.chat-main__messages__info__name
        = message.user.name
    %p.chat-main__messages__info__date
        =  message.created_at.strftime("%Y年%m月%d日 %H時%M分")
    %p.chat-main__messages__message
    - if message.content.present?
        %p.lower-message__content
    = message.content
    = image_tag message.image.url, class: 'lower-message__image' if message.image.present?